spring:
  datasource:
    url: jdbc:h2:mem:springbootexampl;MODE=MYSQL;DB_CLOSE_DELAY=-1;INIT=RUNSCRIPT FROM 'classpath:h2sql/init.sql'
    driver-class-name: org.h2.Driver
logback:
  username: logback
  password: '{cipher}Encrypted Text Value from secret and Public Key'
encrypt:
  key: 'Private Key In PEM Encoding'